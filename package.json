{
  "name": "traccal",
  "version": "1.0.0",
  "private": true,
  "description": "A private, offline-first calorie calculator and tracker.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "check": "node --check app.js && node --check service-worker.js"
  }
}
